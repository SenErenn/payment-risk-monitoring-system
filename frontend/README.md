# PayScope Frontend

React + TypeScript + Vite UI for the Payment Risk Monitoring System.

## Scripts

```bash
npm install
npm run dev      # http://localhost:5173
npm run build
npm run lint
```

Set `VITE_API_BASE_URL` (default `http://localhost:5067`). See the root README for full setup and demo users.
